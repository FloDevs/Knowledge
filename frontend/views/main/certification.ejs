<%- include('../partials/header.ejs', { pageTitle: 'Mes certifications' }) %>

<h2>Mes certificats</h2>

<% if (certifications.length === 0) { %>
  <p>Vous n'avez pas encore obtenu de certificat.</p>
<% } else { %>
  <ul>
    <% certifications.forEach(certif => { %>
      <li>
        <strong><%= certif.cursus.title %></strong> - Délivré le <%= new Date(certif.issuedAt).toLocaleDateString('fr-FR') %>
        <a href="/certifications/download/<%= certif.cursus._id %>">📄 Télécharger</a>
      </li>
    <% }) %>
  </ul>
<% } %>

<%- include('../partials/footer.ejs') %>